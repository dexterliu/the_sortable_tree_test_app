== TheSortableTree Test Application

== Ruby 1.8.7 + Rails 3.1.0 + TheSortableTree

1. git clone
2. bundle
3. config/database.yml.example => config/database.yml
4. rake db:create && rake db:migrate && rake db:seed
5. rails s
6. localhost:3000